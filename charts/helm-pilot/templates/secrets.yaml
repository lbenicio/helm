{{- if .Values.config.oidc.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm-pilot.fullname" . }}-oidc
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "helm-pilot.labels" . | nindent 4 }}
type: Opaque
stringData:
  clientId: {{ .Values.config.oidc.clientId | quote }}
  clientSecret: {{ .Values.config.oidc.clientSecret | quote }}
{{- end }}
---
{{- if .Values.config.geminiApiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm-pilot.fullname" . }}-gemini
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "helm-pilot.labels" . | nindent 4 }}
type: Opaque
stringData:
  apiKey: {{ .Values.config.geminiApiKey | quote }}
{{- end }}
