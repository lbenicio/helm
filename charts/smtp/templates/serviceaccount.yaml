{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "smtp.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "smtp.labels" . | nindent 4 }}
{{- end }}
