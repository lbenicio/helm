{{- if .Values.config.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "whoami.fullname" . }}-config
  labels:
    {{- include "whoami.labels" . | nindent 4 }}
data:
  {{- with .Values.config.data }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
